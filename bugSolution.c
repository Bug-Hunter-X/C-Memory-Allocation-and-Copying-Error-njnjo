int main() {    int arrSize = 5;    int arr[5] = {1, 2, 3, 4, 5};    int *ptr = (int *)malloc(sizeof(int) * arrSize);    if (ptr == NULL) {        fprintf(stderr, "Memory allocation failed\n");        return 1; // Handle memory allocation failure    }    memcpy(ptr, arr, sizeof(int) * arrSize);  // Correctly copy using size    free(ptr);    ptr = NULL; // Avoid dangling pointer    return 0;} 